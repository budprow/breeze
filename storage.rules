rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // TEMPORARY DEBUGGING RULE:
    // This rule ignores the folder structure and simply checks
    // if the user is logged in.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}